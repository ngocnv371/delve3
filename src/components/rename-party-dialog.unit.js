import RenamePartyDialog from './rename-party-dialog'

describe('@components/rename-party-dialog', () => {
  it('exports a valid component', () => {
    expect(RenamePartyDialog).toBeAComponent()
  })
})
