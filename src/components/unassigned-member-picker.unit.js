import UnassignedMemberPicker from './unassigned-member-picker'

describe('@components/unassigned-member-picker', () => {
  it('exports a valid component', () => {
    expect(UnassignedMemberPicker).toBeAComponent()
  })
})
